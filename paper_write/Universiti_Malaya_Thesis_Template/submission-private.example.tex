% Copy this file to submission-private.tex and fill it only on the private
% submission machine.  The destination filename is ignored by Git.
\renewcommand{\candidatePassportNo}{REPLACE LOCALLY}
\renewcommand{\candidateMatricNo}{REPLACE LOCALLY}
